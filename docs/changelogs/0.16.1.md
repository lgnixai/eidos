## Commits

504c263d feat(ext): Add 'Run' button to extension toolbar for executing scripts and refactor script call handling
405f1545 refactor(ext): Update response handling in useExtMsg hook to return text directly from AI model
6866e2ba refactor(chat): Improve error handling and configuration retrieval for AI models across chat components
1257ace6 Update to version 0.16.1
709e01f1 refactor(dependencies): Optimize lodash imports for better tree-shaking and performance across various components
de41e34d feat(search): Introduce enableFTS option for Full Text Search functionality in DataSpace and related components
a751909a feat(ext): Add error handling for script calls and refactor AI model retrieval
faa1fb5d feat(types): Add script and AI functionality to Eidos interface with detailed type definitions
df492f96 refactor(types): Update type definitions and enhance type safety across various modules
47812b0e refactor(core): Replace isServer flag with isUDFWithCtx in DataSpace for improved context handling
e2faf38a refactor(core): Update BaseServerDatabase interface and implement SQLiteWasmDatabase for improved database handling
a3bd4e5b feat(protocol): Implement block action handling in protocol handler and update URL opening logic
4146783f fix(doc): Enhance component picker with mblock filtering and UI improvements
e5688c11 fix(core): Update BroadcastChannel name for draft db
aa6755a2 feat(search): Enable full text search in ink service mode
bf3c38e1 fix(file): Improve file cell UI and file path matching logic
7d58c51f refactor(script): Simplify proxy function creation for table and method handling
b016cd3a refactor(core): Standardize BroadcastChannel usage and update core version
fc27b232 feat(file): Improve file handling and preview with comma encoding and data URI support
278e25be feat(gallery): Add fit content option for gallery view cover images
22532df5 feat(fields): Improve field type detection and display type handling

---

# v0.16.1 Changelog

This is a minor version update with some fixes and improvements.

## Fixes and Improvements

### File Field

- File fields can now display data URI images.
- Formula fields displayed as files, or lookup fields targeting file fields, can now be used as gallery covers.
- Gallery covers can now be set to "fit content", automatically adjusting image size based on aspect ratio.

### Micro Block

- Improved document menu selection - Micro Blocks can now be properly filtered and are arranged after built-in blocks.
- Blocks can now be opened independently via the `eidos://block/<blockId>@<spaceId>` URI schema.

### Script

- Scripts can now be run directly.
- Updated Eidos type definitions for better type hints in the extension editor.
  - Scripts can now call AI models
  ```ts
  // scritpA
  export default async function (input: Input, context: Context) {
    const result = await eidos.AI.generateText({
      model: "google/gemini-2.0-flash-001@openrouter",
      prompt: input.prompt || "9.11 and 9.9, which one is larger?",
    })
    eidos.currentSpace.notify({
      title: "AI response from scriptA",
      description: result,
    })
    return result
  }
  ```
  - Scripts can now call other scripts
  ```ts
  // scriptB
  export default async function (input: Input, context: Context) {
    await eidos.script.call("scriptA", {
      prompt: `How many r's are in the word strawberry?`,
    })
  }
  ```

## Refactor

- Core: DataSpace now has fewer runtime API dependencies
